// Declares the structure for bubble animation items reused across the UI.
export interface Bubble {
  id: number;
  size: number;
  left: number;
  animationDuration: number;
}
export type BubbleDefinition = {
  id: number;
  size: number;
  left: number;
  animationDuration: number;
};

